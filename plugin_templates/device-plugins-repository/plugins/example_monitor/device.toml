id = "example_monitor"
name = "Example Read-only Monitor"
version = "0.1.0"
api_version = "1.1"
core_requires = ">=0.14,<0.15"
backend = "backend:ExampleMonitor"
kinds = ["monitor"]
dependencies = []
